# PyChronicle

Week 2 traces every executed line of `sample.py`, saves frame state to SQLite,
and provides a static Textual debugger layout.

```powershell
cd pychronicle
py -m pip install -r requirements.txt
py main.py
py -m unittest test_week2.py
```
